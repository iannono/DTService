ToDo List

* 针对pincome和hubincome修改为直接删除原数据，再导入新的数据
* 导入的时候，最好判断一下当前的文件数据是否是和文件名的月份是匹配的，判断第一条数据就好，如果不对，给予相应的提示，停止导入文件
* wufast这张表的原始数据中是没有重组后承运人的，需要根据承运人的类别来动态的写入，对应关系是：CA 国航 国深航，ZH 深航 国深航， FM 上航 东上航， MU 东航 东上航，对于没有对应的，就直接写承运人中文
* ----wufast中的周一、年月这种数据需要提取其中的数字，存入到数据库，表中对应的字段是int
* et的数据需要由直接插入改成更新，并且有可能一个文件含有很多天的数据，所以需要根据文件的数据的日期来做判断，并写入数据
* ----lineincome的两个字段的顺序需要修改，zuogongliincome表示不含燃油，zuogonglioil表示含燃油
* ----data目录下的数据文件目录改成对应的中文

